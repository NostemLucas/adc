/**
 * Query para listar todos los usuarios activos.
 * Puede extenderse con filtros, paginación, etc.
 */
export class ListUsersQuery {
  // Puede recibir parámetros de filtrado/paginación en el futuro
  constructor() {}
}
