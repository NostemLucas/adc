/**
 * System Resources
 *
 * Represents the main resources/modules in the system.
 * Each resource can have actions performed on it.
 */
export enum Resource {
  USERS = 'users',
  ROLES = 'roles',
  AUDITS = 'audits',
  FINDINGS = 'findings',
  REPORTS = 'reports',
  CLIENTS = 'clients',
  SETTINGS = 'settings',
  NOTIFICATIONS = 'notifications',
}
